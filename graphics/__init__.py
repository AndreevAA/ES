class Graphics:

    legend, labels, values = None, None, None

    def __init__(self, legend, labels, values):
        self.legend = legend
        self.labels = labels
        self.values = values

